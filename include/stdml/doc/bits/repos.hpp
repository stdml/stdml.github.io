#pragma once
#include <string>
#include <vector>

using slist = std::vector<std::string>;

const slist stdml_repos({
    "stdtensor",
    "stdnn-ops",
    "stdnn-ops-cuda",
    "nn-graph-experimental",
    "stdtracer",
});
